# Placeholder-only local configuration.
# Do not put production secrets, OAuth client secrets, refresh tokens, real user emails,
# deployed URLs, or real health data in this repository.

NODE_ENV=development
PORT=3000
GOOGLE_OAUTH_CLIENT_ID=replace-with-placeholder-client-id
GOOGLE_OAUTH_CLIENT_SECRET=replace-with-placeholder-client-secret
GOOGLE_OAUTH_REDIRECT_URI=https://example.invalid/oauth/callback
